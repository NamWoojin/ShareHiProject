'use strict'

const basic = 'select * from member where mem_email=? and mem_password=?';
const socail = 'select * from member where mem_email=? and mem_password=?';

module.exports = {
    basic,
    socail
}